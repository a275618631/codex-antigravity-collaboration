#!/usr/bin/env bash
set -euo pipefail

echo "This script no longer pulls settings automatically."
echo "Use 手動拉取分類設定.sh with explicit --category choices instead."
echo "Example:"
echo "  ./同步工具/手動拉取分類設定.sh --pull --source YOUR-MAC --category agents-root --category skills --union"
